public class arithmetic {
    public static void main(String[] args) {
        Math.max(5,10); // this prints out the highest value of x and y. An example would be Math.max(x,y);
        System.out.println(Math.max(5,10)); // this should then print 10 since its the highest number
    }
    
}
